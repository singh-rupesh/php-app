<?php

// Variable = (Condition) ? (Statement1) : (Statement2);

$a = 10;
$b = 10;

$sth = ($a == $b) ? ("Value of a and b are same") : ("Value of a and b are not same");

echo $sth;

// for single condition